<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="studio.rockpile.devtools.dao.UserMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="studio.rockpile.devtools.entity.User">
        <id column="id" property="id" />
        <result column="user_name" property="userName" />
        <result column="password" property="password" />
        <result column="login_name" property="loginName" />
        <result column="telephone" property="telephone" />
        <result column="regist_time" property="registTime" />
        <result column="random_salt" property="randomSalt" />
        <result column="tenant_id" property="tenantId" />
        <result column="is_disabled" property="isDisabled" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, user_name, password, login_name, telephone, regist_time, random_salt, tenant_id, is_disabled
    </sql>

</mapper>
